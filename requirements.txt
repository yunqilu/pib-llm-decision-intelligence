# Core (env, harness, optimization baselines)
numpy>=1.24
jsonschema>=4.0

# optimization/ MILP/LP track (see docs/decisions/2026-07-16-solver-choice.md)
pyomo>=6.0
highspy>=1.5

# rl/ track (optional -- only needed for rl/gym_wrapper.py)
gymnasium>=0.29

# dev
pytest>=7.0
